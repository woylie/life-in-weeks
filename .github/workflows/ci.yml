name: CI

on:
  push:
  pull_request:
    types: [opened, edited]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 16
          cache: "yarn"
      - run: yarn
      - run: yarn format --validate
      - run: yarn build
